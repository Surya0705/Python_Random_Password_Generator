from password_generator import PasswordGenerator # Importing the module.

a = PasswordGenerator() # Initialising the generator.
b = a.generate() # Genearting a strong Password.
print(f"Your generated Password is {b}") # Displaying the Password to the User.